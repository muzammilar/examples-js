# examples-js
Example codes in javascript, typescript, and even Node.js for fun. This repo is for anything that remotely looks like javascript.

All examples should have associated `Dockerfile` and `docker-compose.yml` files for experimenting and development.
